{% extends 'StepiiikZfmeetupBundle::layout.html.twig' %}

{% block content %}

<h1>Editace alba</h1>

<p>
    <a href="{{ path('route.album') }}">
        Zpět na seznam...
    </a>
</p>

<form action="{{ path('route.album_edit', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }} novalidate>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit" class="btn btn-large">Editovat</button>
    </p>
</form>

<p class="record_actions">
    <form action="{{ path('route.album_delete', { 'id': entity.id }) }}" method="post">
        {{ form_widget(delete_form) }}
        <button type="submit" class="btn">Smazat</button>
    </form>
</p>

{% endblock %}