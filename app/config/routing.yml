stepiiik:
    resource: "@StepiiikZfmeetupBundle/Controller/"
    type:     annotation
    prefix:   /

route.login_check:
    pattern:   /login_check

route.logout:
    pattern:   /logout

# Internal routing configuration to handle ESI
#_internal:
#    resource: "@FrameworkBundle/Resources/config/routing/internal.xml"
#    prefix:   /_internal
